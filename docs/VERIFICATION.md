# 验证记录

验证日期：2026-08-14。

## 自动检查

运行 `scripts/check.ps1`：

- 后端 61 项测试通过。
- TypeScript 类型检查通过。
- Vite 生产构建通过，1580 个模块完成转换。
- 硬编码凭证模式扫描通过。

测试覆盖签名、模拟盘标头、私有端点白名单、cash/tag 固定、基础风控、审计脱敏与审计链篡改锁定、持久急停字段严格解析、撤单内层错误码、订单 `50004`、传输与畸形响应歧义、订单回显身份校验、公共行情品种/价格/时间戳与 K 线结构、并发提交单次派发、崩溃恢复、急停代次与复位竞态、下单前线性化急停门、API Key/账户 UID 身份绑定、跨进程单账户事务约束、多账户潜在订单拒绝解锁、未决订单逐笔终态核对、挂单列表分页、CAA 响应/时间新鲜度/本地租约与单实例限频、在途续租和急停的最终写入顺序，以及下单、回查和 CAA 启动任务被取消时的 fail-closed 行为。

这些测试是离线单元与故障编排测试，不是 OKX 私有模拟盘端到端订单验证。

## 只读运行检查

- `environment = OKX 模拟盘`
- `mode = observe`
- `credentialConfigured = false`
- `auditChainValid = true`
- OKX 返回 `BTC-USDT / SPOT`
- 公共行情来源为 `OKX 公共行情`
- 返回 96 根 5 分钟 K 线
- 本轮没有调用私有 OKX API，没有下单或撤单

## 浏览器验收

- 桌面：`clientWidth = 1415`、`scrollWidth = 1415`，无页面级横向溢出。
- 移动：显式设置 375×812 视口，文档 `clientWidth = 360`、`scrollWidth = 360`，无页面级横向溢出。
- 移动端 6 个导航入口全部存在，“设置与安全边界”可进入。
- 未配置凭证时启用和下单预检均禁用。
- 空数量不会显示成虚构的 `0.0000 USDT`，而是 `—`。
- 急停弹窗焦点进入对话框，Escape 能关闭；未执行确认急停。
- 桌面浏览器控制台没有 error 或 warn。

## 尚未验证

- 用户的 OKX Demo API 凭证尚未写入本程序的 Windows Credential Manager。
- 未运行私有账户读取、模拟盘下单、查询、部分成交或撤单 smoke test。
- 未集成 FreqAI、模型训练、回测或自动策略调度。
- 没有正式盘代码路径，也未做正式盘验证。
